
Before doing anything, make sure you have the HomeBrew package manager installed on your Mac.

Then install the following packages:

.. code-block::

   #!shell
   brew install cmake
   brew install geos
