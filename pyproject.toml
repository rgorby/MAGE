[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lumache"
authors = [{name = "Eric Winter", email = "eric.winter@jhuapl.edu"}]
dynamic = ["0.0", "Kaiju documentation"]
